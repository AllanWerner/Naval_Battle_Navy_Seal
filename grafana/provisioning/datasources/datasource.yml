apiVersion: 1

datasources:
  # http://prometheus:9090 : le conteneur Grafana résout Prometheus par
  # son nom de service Compose sur le network partagé, jamais localhost.
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
